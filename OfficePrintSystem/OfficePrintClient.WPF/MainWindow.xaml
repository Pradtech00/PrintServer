<Window x:Class="OfficePrintClient.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Office Print Client" Height="650" Width="950"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" FontSize="13">
    <Window.Resources>
        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="SectionHeader">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
        <Style TargetType="Border" x:Key="DropZone">
            <Setter Property="BorderBrush" Value="#0078D4"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Background" Value="#F5F5F5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0078D4" CornerRadius="6" Padding="16" Margin="0,0,0,10">
            <TextBlock Text="Office Print System - Client Dashboard" FontSize="20"
                       FontWeight="Bold" Foreground="White"/>
        </Border>

        <!-- Connection Panel -->
        <Border Grid.Row="1" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,8">
            <StackPanel>
                <TextBlock Text="Server Connection" Style="{StaticResource SectionHeader}"/>
                <WrapPanel Margin="0,4,0,0">
                    <TextBlock Text="Server IP:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox x:Name="ServerIpBox" Width="220" Text="192.168.1.100" VerticalAlignment="Center"/>
                    <Button x:Name="ConnectBtn" Content="Connect" Style="{StaticResource PrimaryButton}"
                            Click="ConnectBtn_Click" Background="#107C10"/>
                    <Button x:Name="RefreshBtn" Content="Refresh Printers" Style="{StaticResource PrimaryButton}"
                            Click="RefreshBtn_Click" IsEnabled="False"/>
                    <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="#666"/>
                </WrapPanel>
                <WrapPanel Margin="0,8,0,0">
                    <TextBlock Text="Select Printer:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox x:Name="PrinterCombo" Width="300" DisplayMemberPath="DisplayName"
                              IsEnabled="False" VerticalAlignment="Center"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- Send Print Job -->
        <Border Grid.Row="2" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,8">
            <StackPanel>
                <TextBlock Text="Send Print Job" Style="{StaticResource SectionHeader}"/>
                <Border x:Name="DropZone" Style="{StaticResource DropZone}" AllowDrop="True"
                        Drop="DropZone_Drop" DragOver="DropZone_DragOver" Height="80">
                    <TextBlock x:Name="DropText" Text="Drag &amp; drop a PDF file here to print"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               FontSize="14" Foreground="#888"/>
                </Border>
            </StackPanel>
        </Border>

        <!-- Job History -->
        <Border Grid.Row="3" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Print Job History" Style="{StaticResource SectionHeader}" Grid.Row="0"/>
                <DataGrid x:Name="JobGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                          AlternatingRowBackground="#F0F0F0" Margin="0,4,0,0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Document" Binding="{Binding DocumentName}" Width="200"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                        <DataGridTextColumn Header="Client" Binding="{Binding ClientMachineName}" Width="120"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding CreatedAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="160"/>
                        <DataGridTextColumn Header="Error" Binding="{Binding ErrorMessage}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Status Bar -->
        <StatusBar Grid.Row="4" Margin="0,6,0,0">
            <StatusBarItem>
                <TextBlock x:Name="StatusBarText" Text="Ready" Foreground="#666"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
